% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myboot2.R
\name{myboot2}
\alias{myboot2}
\title{Bootstrap confidence interval for a statistic}
\usage{
myboot2(iter = 10000, x, fun = mean, alpha = 0.05, cx = 1.5, ...)
}
\arguments{
\item{iter}{Integer. Number of bootstrap iterations. Default is 10000.}

\item{x}{Numeric vector. The sample data to bootstrap.}

\item{fun}{Function or character. Statistic to compute (e.g. \code{mean}, \code{median}). Default is \code{mean}.}

\item{alpha}{Numeric in (0,1). Significance level for the CI. Default is 0.05 (95\% CI).}

\item{cx}{Numeric. Scaling factor for text in the plot. Default is 1.5.}

\item{...}{Additional graphical parameters passed to \code{hist()} (e.g., \code{xlab}, \code{col}, \code{main}).}

\item{seed}{Optional integer. If provided, sets a seed for reproducible resampling.}
}
\value{
A (invisible) list with components:
\describe{
  \item{ci}{Numeric vector of length 2: lower and upper percentile bootstrap CI.}
  \item{fun}{The function used for the statistic.}
  \item{x}{The original sample vector.}
  \item{xstat}{Numeric vector of bootstrap statistics.}
  \item{pte}{Point estimate of the statistic from the original sample.}
  \item{iter}{Number of bootstrap iterations used.}
  \item{alpha}{Alpha level used.}
}
}
\description{
Compute a percentile bootstrap confidence interval for a statistic and plot
a histogram of the bootstrap replicates.
}
\examples{
myboot2(iter = 1000, x = rnorm(30), fun = mean, alpha = 0.05,
        xlab = "Bootstrap mean", col = "purple", cx = 1.2)
}
